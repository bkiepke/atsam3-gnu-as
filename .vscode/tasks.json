{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build all",
            "command": "/usr/bin/make",
            "args": [ "-f", "${workspaceFolder}/Makefile", "all" ],
            "options": {
                "cwd": "/usr/bin"
            },
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}